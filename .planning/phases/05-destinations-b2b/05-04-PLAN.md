---
phase: 05-destinations-b2b
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified: []
autonomous: false
requirements:
  - DEST-01
  - DEST-02
  - DEST-03
  - DEST-04
  - DEST-05
  - DEST-06
  - DEST-07
  - B2B-01
  - B2B-02
  - B2B-03
  - B2B-04
  - B2B-05
  - B2B-06

must_haves:
  truths:
    - "All 6 destination slugs load their correct hero, editorial content, and sections"
    - "An invalid destination slug returns a 404 redirect"
    - "A destination with no published trips shows the waitlist form instead of trip cards"
    - "The B2B page renders all sections correctly and shows WhatsApp fallback when TALLY_B2B_URL is empty"
    - "The /destinazioni listing page loads with 6 destination cards linking to their respective pages"
  artifacts:
    - path: "destinazione.php"
      provides: "Verified working for all 6 slugs"
    - path: "agenzie.php"
      provides: "Verified working with all B2B sections visible"
    - path: "destinazioni.php"
      provides: "Verified as a working destination index"
  key_links:
    - from: "destinazione.php"
      to: "includes/destinations-data.php"
      via: "require_once"
    - from: "agenzie.php"
      to: "TALLY_B2B_URL/WHATSAPP_B2B_FALLBACK"
      via: "config.php conditional"
---

<objective>
Human verification of all Phase 5 pages: 6 destination slugs, invalid slug 404, waitlist form behavior, and the full B2B page.

Purpose: Confirms the editorial content renders correctly in a real browser, responsive layout looks correct on mobile, and interactive behaviors (waitlist form, Tally fallback) work as expected.
Output: Verified Phase 5 pages, ready for Phase 6
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-destinations-b2b/05-02-SUMMARY.md
@.planning/phases/05-destinations-b2b/05-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Browser verification of all Phase 5 pages</name>
  <action>
    Human verifies all destination pages, the B2B page, the destination listing, and confirms no regressions on existing pages.
  </action>
  <verify>Human sign-off via "approved" signal</verify>
  <done>All 11 checklist items confirmed passing by human reviewer</done>
  <what-built>
    Complete Phase 5 output:
    - destinazione.php: single template for 6 editorial destination pages (hero, breadcrumb, editorial intro, practical info boxes, "Cosa Vedere" 4-card grid, "Curiosità" 3-card section, trips grid or waitlist form)
    - api/submit-waitlist.php: cURL webhook for waitlist form submissions
    - agenzie.php: full B2B partnership page (hero, trust bar, 3 value prop cards, how-it-works steps, written guarantee block, testimonial, Tally/WhatsApp form)
    - destinazioni.php: minimal destination listing page (6 cards linking to each destination)
    - Phase 5 CSS block appended to style.css
    - 3 new constants in config.php (WAITLIST_WEBHOOK_URL, TALLY_B2B_URL, WHATSAPP_B2B_FALLBACK)
  </what-built>
  <how-to-verify>
    Test each item in the browser:

    **Destination pages — all 6 slugs:**
    1. Visit /destinazione/america — verify: hero loads with photo, breadcrumb shows "Home › Destinazioni › America", 3 intro paragraphs visible, 5 practical info boxes (Valuta, Lingua, Stagione, Fuso Orario, Visto), 4 "Cosa Vedere" cards, 3 "Curiosità" cards with gold left border
    2. Repeat spot-check for /destinazione/asia, /destinazione/europa (at minimum)
    3. Visit /destinazione/medio-oriente — verify the hyphenated slug works
    4. Visit /destinazione/invalid-slug — verify redirect to 404 page

    **Waitlist form (DEST-07):**
    5. On a destination with no published trips (temporarily unpublish all trips for one continent, or use a continent that has none), verify the waitlist dark box appears instead of trip cards
    6. Submit the waitlist form with nome/email/telefono — verify inline success message appears without page reload

    **B2B page:**
    7. Visit /agenzie — verify: dark hero loads with "Diventa Agenzia Partner" headline, trust bar shows 4 items, 3 value prop cards render, how-it-works shows 3 numbered steps, "I tuoi clienti restano tuoi" guarantee block has gold left border, fictional testimonial renders
    8. Verify TALLY_B2B_URL is empty in config.php (it is, by default) — confirm WhatsApp fallback button renders in the registration section

    **Destination listing:**
    9. Visit /destinazioni — verify 6 destination cards render with photos and names, each clicking through to the correct destination page

    **No regressions:**
    10. Visit homepage (/) — confirm existing pages still look correct (no CSS collisions from Phase 5 block)
    11. Visit /viaggi — confirm catalog still works correctly
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Describe any visual issues or broken behaviors if not.</resume-signal>
</task>

</tasks>

<verification>
Human visual verification as described above — covers all DEST-* and B2B-* requirements.
</verification>

<success_criteria>
- All 6 destination slugs load with correct content
- Invalid slug 404s correctly
- Destinations with no published trips show waitlist form; waitlist form submits successfully
- B2B page has all required sections and shows WhatsApp fallback (TALLY_B2B_URL empty by default)
- /destinazioni listing works and fixes the footer link
- No visual regressions on existing pages (homepage, catalog, trip detail)
</success_criteria>

<output>
After completion, create `.planning/phases/05-destinations-b2b/05-04-SUMMARY.md`
</output>

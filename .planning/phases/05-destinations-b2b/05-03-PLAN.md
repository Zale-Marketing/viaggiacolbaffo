---
phase: 05-destinations-b2b
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - agenzie.php
  - destinazioni.php
autonomous: true
requirements:
  - B2B-01
  - B2B-02
  - B2B-03
  - B2B-04
  - B2B-05
  - B2B-06

must_haves:
  truths:
    - "Visiting /agenzie loads the full B2B page: dark hero, trust bar, 3 value prop cards, how-it-works steps, client guarantee block, testimonial, and Tally form (or WhatsApp fallback)"
    - "When TALLY_B2B_URL is empty in config.php, the WhatsApp fallback button renders instead of the Tally iframe"
    - "Commission language on agenzie.php never mentions a specific percentage — only 'commissioni competitive'"
    - "Visiting /destinazioni loads a minimal page listing all 6 destination cards (fixes the broken footer link)"
    - "The B2B written guarantee text matches the copy from viaggiacolbaffo.com/diventa-agenzia-partner/"
  artifacts:
    - path: "agenzie.php"
      provides: "B2B agency partnership page"
      contains: "b2b-value-card"
    - path: "destinazioni.php"
      provides: "Minimal destination listing page"
      contains: "destinations-data.php"
  key_links:
    - from: "agenzie.php"
      to: "includes/config.php"
      via: "defined('TALLY_B2B_URL') && TALLY_B2B_URL"
      pattern: "TALLY_B2B_URL"
    - from: "agenzie.php"
      to: "includes/config.php"
      via: "defined('WHATSAPP_B2B_FALLBACK')"
      pattern: "WHATSAPP_B2B_FALLBACK"
---

<objective>
Build agenzie.php (the B2B agency partnership page) and destinazioni.php (a minimal destination listing that fixes the broken footer link). These two pages are independent from destinazione.php and can be built in parallel with Plan 02.

Purpose: Fulfills all B2B-* requirements. Fixes the footer's /destinazioni link that has been broken since Phase 2.
Output: agenzie.php (full B2B page), destinazioni.php (minimal destination index)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-destinations-b2b/05-CONTEXT.md
@.planning/phases/05-destinations-b2b/05-RESEARCH.md
@.planning/phases/05-destinations-b2b/05-01-SUMMARY.md

<interfaces>
<!-- Contracts this plan builds against -->

B2B written guarantee copy (from live site, verified in RESEARCH.md):
```
Headline: "I tuoi clienti restano tuoi, sempre"

Guarantee text:
"Non contatteremo mai direttamente i tuoi clienti per proporre i nostri servizi."
"Se in futuro un tuo cliente dovesse prenotare direttamente con noi (senza passare
dalla tua agenzia), ti riconosceremo comunque la tua commissione."

Trust bar points:
- "Commissioni interessanti su ogni prenotazione confermata"
- "I tuoi clienti restano tuoi, sempre"
- "Garanzia Scritta"
- "Supporto dedicato"
- "Preventivi in 24h"
```

Tally embed pattern (from RESEARCH.md Pattern 5):
```php
<?php if (defined('TALLY_B2B_URL') && TALLY_B2B_URL): ?>
  <iframe src="<?= htmlspecialchars(TALLY_B2B_URL) ?>"
          width="100%" style="min-height:500px;border:none;" frameborder="0">
  </iframe>
<?php elseif (defined('WHATSAPP_B2B_FALLBACK') && WHATSAPP_B2B_FALLBACK): ?>
  <a href="<?= htmlspecialchars(WHATSAPP_B2B_FALLBACK) ?>"
     class="btn btn--gold" target="_blank" rel="noopener">
    <i class="fa-brands fa-whatsapp"></i> Contattaci su WhatsApp
  </a>
<?php endif; ?>
```

Hero page pattern (both pages use this):
```php
$hero_page = true;
require_once ROOT . '/includes/header.php';
```

CSS classes available (from Plan 01, style.css Phase 5 block):
- .b2b-trust-bar, .b2b-trust-bar__list, .b2b-trust-bar__item
- .b2b-value-grid, .b2b-value-card, .b2b-value-card__icon, .b2b-value-card__title, .b2b-value-card__text
- .b2b-steps, .b2b-step, .b2b-step__number, .b2b-step__title, .b2b-step__text
- .b2b-guarantee, .b2b-guarantee__title, .b2b-guarantee__text
- .b2b-tally-wrap

Existing layout classes: .section, .section--dark, .container, .section-header, .section-header__title
Existing button classes: .btn--gold, .btn--outline-white
Testimonial: .testimonial-card (from homepage Phase 2 — reuse for agency testimonial)

From includes/destinations-data.php (for destinazioni.php):
```php
// $destinations array keyed by slug, each has: name, hero_image
// Use these to render 6 destination cards on destinazioni.php
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agenzie.php — full B2B agency partnership page</name>
  <files>agenzie.php</files>
  <action>
Create `agenzie.php` in the project root. Structure:

**PHP header:**
```php
<?php
require_once __DIR__ . '/includes/config.php';
require_once ROOT . '/includes/functions.php';

$page_title = 'Diventa Agenzia Partner — Viaggia Col Baffo';
$meta_description = 'Unisciti alla rete di agenzie partner di Viaggia Col Baffo. Commissioni competitive, catalogo pronto da vendere, clienti sempre tuoi. Garanzia scritta.';

$hero_page = true;
require_once ROOT . '/includes/header.php';
?>
```

**Section 1 — Hero (B2B-01):**
Dark full-viewport hero using $hero_page = true pattern. Use a dramatic Unsplash travel/business photo (warm handshake or travel agency vibe). Hero content:
- H1: "Diventa Agenzia Partner" (Playfair, large)
- Subline: "Cresciamo insieme — porta i tuoi clienti a scoprire il mondo con Lorenzo" (or similar warm/inviting line matching the brand tone)
- Two CTAs: "Registrati ora" (btn--gold, anchors to #registration section) + "Scopri come funziona" (btn--outline-white, anchors to #come-funziona)

**Section 2 — Trust Bar (B2B-02):**
Immediately after hero (no section wrapper needed — or use a thin .section with minimal padding):
```html
<div class="b2b-trust-bar">
  <div class="container">
    <ul class="b2b-trust-bar__list">
      <li class="b2b-trust-bar__item"><i class="fa-solid fa-shield-halved"></i> Garanzia scritta</li>
      <li class="b2b-trust-bar__item"><i class="fa-solid fa-percent"></i> Commissioni competitive</li>
      <li class="b2b-trust-bar__item"><i class="fa-solid fa-headset"></i> Supporto dedicato</li>
      <li class="b2b-trust-bar__item"><i class="fa-solid fa-briefcase"></i> Materiali marketing inclusi</li>
    </ul>
  </div>
</div>
```

**Section 3 — Value Proposition Cards (B2B-03):**
Section class .section--dark, section header: "Perché Scegliere Col Baffo".
Three .b2b-value-card cards in .b2b-value-grid:
1. Icon: fa-solid fa-handshake — Title: "I Tuoi Clienti Restano Tuoi" — Text: summarize the guarantee in 2 sentences — use language from RESEARCH.md ("Non contatteremo mai..." principle). NO hardcoded commission percentage.
2. Icon: fa-solid fa-coins — Title: "Commissioni Competitive" — Text: "Guadagna una commissione su ogni prenotazione confermata. La percentuale esatta è definita per ogni viaggio — contattaci per i dettagli." (NEVER write a specific % like 12% — locked decision)
3. Icon: fa-solid fa-folder-open — Title: "Catalogo Pronto da Vendere" — Text: "Accedi immediatamente al nostro catalogo completo di itinerari curati, materiali marketing, e schede viaggio pronte per la vendita."

**Section 4 — How It Works (B2B-04):**
Section class .section, id="come-funziona", section header: "Come Funziona".
Three .b2b-step items in .b2b-steps:
1. Number 1 — Title: "Registrati" — Text: "Compila il modulo qui sotto con i dati della tua agenzia. Risponderemo entro 24 ore."
2. Number 2 — Title: "Ricevi il Catalogo" — Text: "Ti inviamo il catalogo completo con schede viaggio, prezzi e materiali di vendita pronti all'uso."
3. Number 3 — Title: "Inizia a Guadagnare" — Text: "Ogni prenotazione confermata genera una commissione. Semplice, trasparente, garantito per iscritto."

**Section 5 — Written Guarantee (B2B-03 detail / B2B-02 expansion):**
Section class .section--dark. Use .b2b-guarantee component:
```html
<div class="container" style="max-width:720px">
  <div class="b2b-guarantee">
    <h3 class="b2b-guarantee__title">I tuoi clienti restano tuoi, sempre</h3>
    <p class="b2b-guarantee__text">Non contatteremo mai direttamente i tuoi clienti per proporre i nostri servizi.</p>
    <p class="b2b-guarantee__text">Se in futuro un tuo cliente dovesse prenotare direttamente con noi (senza passare dalla tua agenzia), ti riconosceremo comunque la tua commissione.</p>
    <p class="b2b-guarantee__text" style="font-style:italic;color:rgba(255,255,255,0.6);font-size:0.9rem">— Lorenzo Baffo, Fondatore di Viaggia Col Baffo</p>
  </div>
</div>
```

**Section 6 — Agency Testimonial (B2B-06):**
Section class .section. Use .testimonial-card (same class as homepage). Fictional placeholder:
- Stars: 5 gold stars (⭐⭐⭐⭐⭐ or FA fa-star icons)
- Quote: "Lavoro con Viaggia Col Baffo da due anni. I miei clienti tornano sempre entusiasti e io ho la certezza di proporre qualcosa di eccellente. La commissione è puntuale e la collaborazione è trasparente fin dal primo giorno."
- Attribution: "Marco Ferretti — Agenzia Viaggi Ferretti, Milano" (realistic fictional name)
- Note: add HTML comment `<!-- TODO: sostituire con testimonianza reale dopo il lancio -->`

**Section 7 — Registration Form (B2B-05):**
Section class .section--dark, id="registration", section header: "Registra la Tua Agenzia".
Wrap in .b2b-tally-wrap:
```php
<?php if (defined('TALLY_B2B_URL') && TALLY_B2B_URL): ?>
  <iframe src="<?= htmlspecialchars(TALLY_B2B_URL) ?>"
          width="100%" style="min-height:500px;border:none;" frameborder="0"
          title="Modulo registrazione agenzia partner">
  </iframe>
<?php elseif (defined('WHATSAPP_B2B_FALLBACK') && WHATSAPP_B2B_FALLBACK): ?>
  <div style="text-align:center">
    <p style="color:rgba(255,255,255,0.75);margin-bottom:1.5rem">Scrivici su WhatsApp per avviare la collaborazione. Risponderemo entro 24 ore.</p>
    <a href="<?= htmlspecialchars(WHATSAPP_B2B_FALLBACK) ?>"
       class="btn btn--gold" target="_blank" rel="noopener">
      <i class="fa-brands fa-whatsapp"></i> Contattaci su WhatsApp
    </a>
  </div>
<?php else: ?>
  <div style="text-align:center">
    <p style="color:rgba(255,255,255,0.75);margin-bottom:1.5rem">Per registrarti come agenzia partner, scrivici all'indirizzo email indicato nel footer.</p>
  </div>
<?php endif; ?>
```

Footer include: `<?php require_once ROOT . '/includes/footer.php'; ?>`

No closing `?>` at end of file.

**CRITICAL constraints (locked decisions):**
- NEVER write a specific commission percentage (no "12%", no "10%", nothing)
- Commission language is ONLY "commissioni competitive" or "commissioni su ogni prenotazione confermata"
  </action>
  <verify>
Verify file exists with B2B sections:
`grep -c "b2b-value-card\|b2b-step\|b2b-guarantee\|TALLY_B2B_URL\|testimonial-card" agenzie.php`
Expected: at least 5

Verify no hardcoded commission percentage:
`grep -i "[0-9]\+%" agenzie.php`
Expected: no output (exit 1 means pass — no percentage found)
  </verify>
  <done>agenzie.php has all 7 sections (hero, trust bar, value props, how-it-works, guarantee, testimonial, registration form), Tally/WhatsApp fallback works, no commission percentage hardcoded</done>
</task>

<task type="auto">
  <name>Task 2: Create destinazioni.php — minimal destination listing page</name>
  <files>destinazioni.php</files>
  <action>
Create `destinazioni.php` in the project root. This is a minimal (~50 lines) listing page that fixes the broken `/destinazioni` footer link. It does NOT need a hero or editorial sections — just a clean grid of the 6 destination cards.

Structure:
```php
<?php
require_once __DIR__ . '/includes/config.php';
require_once ROOT . '/includes/functions.php';
require_once ROOT . '/includes/destinations-data.php';

$page_title = 'Destinazioni — Viaggia Col Baffo';
$meta_description = 'Scopri le destinazioni di Viaggia Col Baffo: America, Asia, Europa, Africa, Oceania e Medio Oriente. Viaggi di gruppo con Lorenzo.';

$hero_page = false;
require_once ROOT . '/includes/header.php';
?>
```

Page body:
- A `.section` with extra top padding to clear the header (since no hero: add `style="padding-top:8rem"` or similar)
- Section header: "Le Nostre Destinazioni" with subline "Scegli la tua prossima avventura"
- A grid (reuse `.trip-grid` or inline 3-col grid) of 6 destination cards
- Each card: `<a href="/destinazione/[slug]">` wrapping a card with the destination hero image and name overlay
- Reuse `.dest-cosa-card` CSS classes for the card style (already defined in Phase 5 CSS):
  ```html
  <a href="/destinazione/<?= $slug ?>" class="dest-cosa-card">
    <img class="dest-cosa-card__img" src="<?= htmlspecialchars($dest['hero_image']) ?>"
         alt="<?= htmlspecialchars($dest['name']) ?>" loading="lazy"
         style="aspect-ratio:16/9">  <!-- override to 16:9 for listing cards -->
    <div class="dest-cosa-card__body">
      <span class="dest-cosa-card__name"><?= htmlspecialchars($dest['name']) ?></span>
    </div>
  </a>
  ```
- Grid: 3-col desktop (repeat(3, 1fr)), 2-col tablet, 1-col mobile — use inline style on grid wrapper or add a utility class
- Use `foreach ($destinations as $slug => $dest)` to iterate

Footer include: `<?php require_once ROOT . '/includes/footer.php'; ?>`
No closing `?>`.
  </action>
  <verify>
Verify file exists with destination iteration:
`grep -c "destinations-data.php\|foreach.*destinations\|dest-cosa-card" destinazioni.php`
Expected: at least 3
  </verify>
  <done>destinazioni.php renders a 6-card grid of all destinations, each linking to /destinazione/[slug], uses destinations-data.php — the footer /destinazioni link no longer 404s</done>
</task>

</tasks>

<verification>
1. `grep "b2b-value-card" agenzie.php` — confirms value prop cards exist
2. `grep "TALLY_B2B_URL" agenzie.php` — confirms Tally conditional
3. `grep -i "[0-9]\+%" agenzie.php` — must return no output (no hardcoded commission %)
4. `grep "destinations-data.php" destinazioni.php` — confirms destination data loaded
5. `grep "foreach.*destinations" destinazioni.php` — confirms 6-card iteration
</verification>

<success_criteria>
- agenzie.php has all B2B-01 through B2B-06 requirements: hero, trust bar, 3 value prop cards, 3 how-it-works steps, written guarantee, testimonial, Tally/WhatsApp registration form
- Commission % is NEVER hardcoded — only "commissioni competitive" language
- destinazioni.php renders all 6 destinations as clickable cards, resolving the broken footer link
</success_criteria>

<output>
After completion, create `.planning/phases/05-destinations-b2b/05-03-SUMMARY.md`
</output>

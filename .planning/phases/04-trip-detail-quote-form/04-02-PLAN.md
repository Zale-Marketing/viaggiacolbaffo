---
phase: 04-trip-detail-quote-form
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - viaggio.php
autonomous: true
requirements: [TRIP-01, TRIP-02, TRIP-03, TRIP-04, TRIP-05, TRIP-06, TRIP-07]

must_haves:
  truths:
    - "Navigating to /viaggio/west-america-aprile-2026 loads the trip — title, hero image, dates, price, status pill all visible"
    - "Navigating to /viaggio/nonexistent redirects to a 404 page"
    - "Scrolling past the hero reveals the sticky top bar with trip name and CTA button"
    - "The highlights bar shows 4 boxes: date, duration, price, posti status"
    - "The sticky tab nav shows 4 tabs; clicking each scrolls smoothly to the correct section"
    - "The itinerary accordion has Day 1 open by default; clicking another day closes Day 1 and opens the new one"
    - "Includes and excludes render in two columns with green checkmark / red X icons"
  artifacts:
    - path: "viaggio.php"
      provides: "Trip detail page top half: routing, hero, sticky bar, highlights, tabs, accordion, includes/excludes"
      min_lines: 200
      contains: "get_trip_by_slug"
  key_links:
    - from: "viaggio.php"
      to: "includes/functions.php"
      via: "get_trip_by_slug($slug)"
      pattern: "get_trip_by_slug"
    - from: "viaggio.php"
      to: "assets/css/style.css"
      via: "class names: trip-hero, trip-tabs, itinerary, trip-topbar"
      pattern: "trip-hero|trip-tabs|itinerary"
---

<objective>
Create viaggio.php with the top half of the trip detail page: slug routing with 404 fallback, cinematic hero, sticky top bar, highlights bar, sticky tab navigation, itinerary accordion, and includes/excludes section.

Purpose: Delivers TRIP-01 through TRIP-07. This is the core structural skeleton — Plan 03 appends the remaining sections (gallery, tags, related trips, quote form) to the same file.
Output: viaggio.php with PHP routing and sections through the includes/excludes block (plus anchor placeholders for gallery and quote form sections Plan 03 will fill).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-trip-detail-quote-form/04-CONTEXT.md
@.planning/phases/04-trip-detail-quote-form/04-01-SUMMARY.md

<interfaces>
<!-- Existing PHP patterns and functions this plan consumes -->

From includes/functions.php:
  get_trip_by_slug(string $slug): ?array
    Returns trip array with keys: slug, title, continent, status, published, date_start,
    date_end, duration, price_from, hero_image, gallery[], short_description, full_description,
    itinerary[{day, title, description}], included[], excluded[], tags[], form_config{}, webhook_url

From includes/config.php constants:
  ROOT, DATA_DIR, WHATSAPP_NUMBER, DEFAULT_WEBHOOK_URL, OPENAI_API_KEY, TALLY_CATALOG_URL

From includes/header.php:
  Reads $page_title (string) and $hero_page (bool) before include
  body gets class="has-hero" when $hero_page=true
  Header is sticky, transparent on hero pages, turns white at scrollY > 80px (.scrolled class)

Existing class names (PERMANENT from Phase 1):
  .trip-card, .trip-card__image, .trip-card__overlay, .trip-card__continent,
  .trip-card__status, .trip-card__content, .trip-card__title, .trip-card__dates,
  .trip-card__price, .status--confermata, .status--ultimi-posti, .status--sold-out, .status--programmata
  .status-pill, .status-confermata, .status-ultimi-posti (standalone pills)
  .btn, .btn--gold, .btn--outline-white
  .section, .section--dark, .container
  .section-header, .section-header__title

Phase 4 CSS classes added in Plan 01:
  .trip-hero, .trip-hero__overlay, .trip-hero__content, .trip-hero__title, .trip-hero__meta, .trip-hero__cta
  .trip-topbar, .trip-topbar.visible, .trip-topbar__name, .trip-topbar__cta
  .trip-highlights, .trip-highlights__grid, .trip-highlights__item, .trip-highlights__label, .trip-highlights__value
  .trip-tabs, .trip-tabs__nav, .trip-tabs__btn, .trip-tabs__btn.active
  .trip-section, .trip-section--dark
  .itinerary, .itinerary__item, .itinerary__header, .itinerary__day-num, .itinerary__title
  .itinerary__chevron, .itinerary__body, .itinerary__desc, .itinerary__item.open
  .includes-grid, .includes-col__title, .includes-list, .includes-list__icon--yes, .includes-list__icon--no

Status label mapping (Italian display):
  'confermata'   → 'Confermata'
  'ultimi-posti' → 'Ultimi posti'
  'sold-out'     → 'Sold out'
  'programmata'  → 'In programmazione'

.htaccess rewrite rule already set: /viaggio/{slug} maps to viaggio.php?slug={slug}
PHP reads: $slug = $_GET['slug'] ?? ''
404 redirect: header("Location: /404"); exit; (or header with 404 status if 404.php exists)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create viaggio.php — routing, hero, sticky top bar, highlights, tabs</name>
  <files>viaggio.php</files>
  <action>
Create viaggio.php. Write it as a single atomic file (no partial writes or appends). The file covers slug routing through the sticky tab nav section. Plan 03 will append the remaining sections.

Structure:

```php
<?php
require_once __DIR__ . '/includes/config.php';
require_once ROOT . '/includes/functions.php';

// --- Slug routing ---
$slug = $_GET['slug'] ?? '';
if (!$slug) { header("Location: /404"); exit; }

$trip = get_trip_by_slug($slug);
if (!$trip) { header("Location: /404"); exit; }

// --- Page variables ---
$page_title = htmlspecialchars($trip['title']) . ' — Viaggia col Baffo';
$hero_page  = true;

// Status display label
$status_labels = [
    'confermata'   => 'Confermata',
    'ultimi-posti' => 'Ultimi posti',
    'sold-out'     => 'Sold out',
    'programmata'  => 'In programmazione',
];
$status_label = $status_labels[$trip['status']] ?? ucfirst($trip['status']);

// Date formatting helpers
function fmt_date(string $d): string {
    $months = ['', 'Gen', 'Feb', 'Mar', 'Apr', 'Mag', 'Giu', 'Lug', 'Ago', 'Set', 'Ott', 'Nov', 'Dic'];
    [$y, $m, $day] = explode('-', $d);
    return "$day {$months[(int)$m]} $y";
}
$date_display = fmt_date($trip['date_start']) . ' – ' . fmt_date($trip['date_end']);

$form_config = $trip['form_config'] ?? [];
$has_form    = !empty($form_config);

require_once ROOT . '/includes/header.php';
?>

<main>

<!-- ========================================================
     TRIP HERO
     ======================================================== -->
<div class="trip-hero" style="background-image:url('<?php echo htmlspecialchars($trip['hero_image']); ?>')">
  <div class="trip-hero__overlay"></div>
  <div class="trip-hero__content container">
    <div class="status-pill status-<?php echo htmlspecialchars($trip['status']); ?>" style="display:inline-block;margin-bottom:0.75rem;">
      <?php echo htmlspecialchars($status_label); ?>
    </div>
    <h1 class="trip-hero__title"><?php echo htmlspecialchars($trip['title']); ?></h1>
    <div class="trip-hero__meta">
      <span><i class="fa-regular fa-calendar"></i> <?php echo htmlspecialchars($date_display); ?></span>
      <span><i class="fa-regular fa-clock"></i> <?php echo htmlspecialchars($trip['duration']); ?></span>
      <span><i class="fa-solid fa-tag"></i> Da €<?php echo number_format($trip['price_from'], 0, ',', '.'); ?> p.p.</span>
    </div>
    <?php if ($has_form): ?>
    <a href="#richiedi-preventivo" class="trip-hero__cta btn">Richiedi Preventivo</a>
    <?php endif; ?>
  </div>
</div>

<!-- ========================================================
     STICKY TOP BAR (appears after hero on scroll)
     ======================================================== -->
<div class="trip-topbar" id="trip-topbar">
  <span class="trip-topbar__name"><?php echo htmlspecialchars($trip['title']); ?></span>
  <?php if ($has_form): ?>
  <a href="#richiedi-preventivo" class="trip-topbar__cta btn btn--gold">Richiedi Preventivo</a>
  <?php endif; ?>
</div>

<!-- ========================================================
     HIGHLIGHTS BAR
     ======================================================== -->
<div class="trip-highlights">
  <div class="trip-highlights__grid">
    <div class="trip-highlights__item">
      <div class="trip-highlights__label">Date</div>
      <div class="trip-highlights__value"><?php echo htmlspecialchars($date_display); ?></div>
    </div>
    <div class="trip-highlights__item">
      <div class="trip-highlights__label">Durata</div>
      <div class="trip-highlights__value"><?php echo htmlspecialchars($trip['duration']); ?></div>
    </div>
    <div class="trip-highlights__item">
      <div class="trip-highlights__label">Prezzo da</div>
      <div class="trip-highlights__value">€<?php echo number_format($trip['price_from'], 0, ',', '.'); ?></div>
    </div>
    <div class="trip-highlights__item">
      <div class="trip-highlights__label">Disponibilità</div>
      <div class="trip-highlights__value"><?php echo htmlspecialchars($status_label); ?></div>
    </div>
  </div>
</div>

<!-- ========================================================
     STICKY TAB NAVIGATION
     ======================================================== -->
<div class="trip-tabs" id="trip-tabs">
  <nav class="trip-tabs__nav">
    <button class="trip-tabs__btn active" data-target="itinerario">Itinerario</button>
    <button class="trip-tabs__btn" data-target="cosa-include">Cosa Include</button>
    <button class="trip-tabs__btn" data-target="galleria">Galleria</button>
    <?php if ($has_form): ?>
    <button class="trip-tabs__btn" data-target="richiedi-preventivo">Richiedi Preventivo</button>
    <?php endif; ?>
  </nav>
</div>

<!-- ========================================================
     ITINERARY SECTION
     ======================================================== -->
<section class="trip-section" id="itinerario">
  <div class="container">
    <div class="section-header">
      <h2 class="section-header__title">Itinerario</h2>
    </div>
    <div class="itinerary">
      <?php foreach ($trip['itinerary'] as $i => $day): ?>
      <div class="itinerary__item <?php echo $i === 0 ? 'open' : ''; ?>">
        <div class="itinerary__header">
          <div class="itinerary__day-num"><?php echo str_pad($day['day'], 2, '0', STR_PAD_LEFT); ?></div>
          <div class="itinerary__title"><?php echo htmlspecialchars($day['title']); ?></div>
          <i class="fa-solid fa-chevron-right itinerary__chevron"></i>
        </div>
        <div class="itinerary__body">
          <p class="itinerary__desc"><?php echo htmlspecialchars($day['description']); ?></p>
        </div>
      </div>
      <?php endforeach; ?>
    </div>
  </div>
</section>

<!-- ========================================================
     COSA INCLUDE SECTION
     ======================================================== -->
<section class="trip-section trip-section--dark" id="cosa-include">
  <div class="container">
    <div class="section-header">
      <h2 class="section-header__title">Cosa Include</h2>
    </div>
    <div class="includes-grid">
      <div>
        <div class="includes-col__title"><i class="fa-solid fa-circle-check" style="color:#2ecc71;margin-right:0.5rem;"></i>Incluso nel prezzo</div>
        <ul class="includes-list">
          <?php foreach ($trip['included'] as $item): ?>
          <li><i class="fa-solid fa-check includes-list__icon--yes"></i><?php echo htmlspecialchars($item); ?></li>
          <?php endforeach; ?>
        </ul>
      </div>
      <div>
        <div class="includes-col__title"><i class="fa-solid fa-circle-xmark" style="color:#CC0031;margin-right:0.5rem;"></i>Non incluso</div>
        <ul class="includes-list">
          <?php foreach ($trip['excluded'] as $item): ?>
          <li><i class="fa-solid fa-xmark includes-list__icon--no"></i><?php echo htmlspecialchars($item); ?></li>
          <?php endforeach; ?>
        </ul>
      </div>
    </div>
  </div>
</section>
```

After the cosa-include section closing tag, add placeholder comments so Plan 03 can append after them:
```
<!-- GALLERY SECTION — Plan 03 appends here -->
<!-- TAGS SECTION — Plan 03 appends here -->
<!-- RELATED TRIPS — Plan 03 appends here -->
<!-- QUOTE FORM — Plan 03 appends here -->
```

Then close `</main>` and include footer:
```php
<?php require_once ROOT . '/includes/footer.php'; ?>
```

Then add the inline `<script>` block (PHP inline JS pattern per project conventions — no separate .js file):

```javascript
<script>
(function () {
  // --- Sticky top bar ---
  var hero   = document.querySelector('.trip-hero');
  var topbar = document.getElementById('trip-topbar');
  var tabs   = document.getElementById('trip-tabs');

  function onScroll() {
    var heroBottom = hero ? hero.getBoundingClientRect().bottom : 0;
    if (topbar) topbar.classList.toggle('visible', heroBottom < 80);
  }
  window.addEventListener('scroll', onScroll, { passive: true });

  // --- Tab navigation: smooth scroll with offset ---
  document.querySelectorAll('.trip-tabs__btn').forEach(function (btn) {
    btn.addEventListener('click', function () {
      document.querySelectorAll('.trip-tabs__btn').forEach(function (b) { b.classList.remove('active'); });
      btn.classList.add('active');
      var target = document.getElementById(btn.dataset.target);
      if (!target) return;
      var offset = (tabs ? tabs.offsetHeight : 0) + 8;
      var top = target.getBoundingClientRect().top + window.scrollY - offset;
      window.scrollTo({ top: top, behavior: 'smooth' });
    });
  });

  // --- Itinerary accordion (single-open) ---
  // Day 1 body: set initial max-height so it's visible (matches .itinerary__item.open CSS)
  document.querySelectorAll('.itinerary__item.open .itinerary__body').forEach(function (body) {
    body.style.maxHeight = body.scrollHeight + 'px';
  });

  document.querySelectorAll('.itinerary__header').forEach(function (header) {
    header.addEventListener('click', function () {
      var item     = header.parentElement;
      var isOpen   = item.classList.contains('open');
      var allItems = document.querySelectorAll('.itinerary__item');

      // Close all
      allItems.forEach(function (it) {
        it.classList.remove('open');
        it.querySelector('.itinerary__body').style.maxHeight = '0';
      });

      // Open clicked (if it was closed)
      if (!isOpen) {
        item.classList.add('open');
        item.querySelector('.itinerary__body').style.maxHeight =
          item.querySelector('.itinerary__body').scrollHeight + 'px';
      }
    });
  });
})();
</script>
```
  </action>
  <verify>
    <automated>php -l /c/Users/Zanni/viaggiacolbaffo/viaggio.php</automated>
  </verify>
  <done>viaggio.php passes PHP syntax check. File contains get_trip_by_slug, trip-hero, trip-topbar, trip-highlights, trip-tabs, itinerary accordion, and includes/excludes sections. PHP closes with footer include and inline script block.</done>
</task>

</tasks>

<verification>
- `php -l viaggio.php` returns "No syntax errors detected"
- File contains all anchor IDs: itinerario, cosa-include (galleria and richiedi-preventivo placeholders present for Plan 03)
- Accordion JS closes other items when a new one is opened
- Topbar visibility toggled by scroll past hero bottom
</verification>

<success_criteria>
viaggio.php is a syntactically valid PHP file covering TRIP-01 through TRIP-07. Slug routing with 404 fallback is in place. All section IDs are anchored. Plan 03 can append remaining sections without conflicts.
</success_criteria>

<output>
After completion, create `.planning/phases/04-trip-detail-quote-form/04-02-SUMMARY.md`
</output>

---
phase: 04-trip-detail-quote-form
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified: []
autonomous: false
requirements: [TRIP-01, TRIP-02, TRIP-03, TRIP-04, TRIP-05, TRIP-06, TRIP-07, TRIP-08, TRIP-09, TRIP-10, FORM-01, FORM-02, FORM-03, FORM-04, FORM-05, FORM-06]

must_haves:
  truths:
    - "Navigating to /viaggio/west-america-aprile-2026 shows the correct trip page with all sections"
    - "All 16 requirements (TRIP-01 through TRIP-10 and FORM-01 through FORM-06) are manually verified against the live page"
  artifacts:
    - path: "viaggio.php"
      provides: "Complete trip detail page"
      contains: "get_trip_by_slug"
    - path: "api/submit-form.php"
      provides: "Webhook proxy"
      contains: "curl_exec"
    - path: "api/generate-form.php"
      provides: "AI form generator"
      contains: "OPENAI_API_KEY"
  key_links:
    - from: "browser"
      to: "/viaggio/west-america-aprile-2026"
      via: "HTTP GET"
      pattern: "HTTP 200"
---

<objective>
Human verification of the complete Phase 4 implementation. Walk through the trip detail page manually against the 16-item checklist from VALIDATION.md.

Purpose: All TRIP-* and FORM-* requirements are visual or interactive — they cannot be fully verified by PHP lint alone. This checkpoint confirms the full user experience works correctly.
Output: User confirms approval or reports issues for gap closure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-trip-detail-quote-form/04-VALIDATION.md
@.planning/phases/04-trip-detail-quote-form/04-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 4: trip detail page (viaggio.php) with all 10 sections, plus api/submit-form.php webhook proxy and api/generate-form.php AI generator.

    Files created/modified:
    - assets/css/style.css — Phase 4 CSS block appended
    - data/trips.json — West America form_config updated with pricing constants
    - viaggio.php — Full trip detail page (new file)
    - api/submit-form.php — Quote form webhook proxy (new file)
    - api/generate-form.php — AI form_config generator (new file)
  </what-built>
  <how-to-verify>
    Deploy to SiteGround (or test locally with `php -S localhost:8000` from the project root), then verify each item below.

    **TRIP-01 — Slug routing:**
    - Visit /viaggio/west-america-aprile-2026 → should load the West America trip
    - Visit /viaggio/viaggio-inesistente → should redirect to 404

    **TRIP-02 — Hero:**
    - Hero image fills the full viewport
    - Title "West America Aprile 2026" is visible in large Playfair text
    - Dates, duration, price, and status pill ("Ultimi posti") are visible
    - "Richiedi Preventivo" CTA button is present (if form_config is non-empty)

    **TRIP-03 — Sticky top bar:**
    - Scroll past the hero → a sticky white bar appears at the top with the trip name and a "Richiedi Preventivo" button
    - Scroll back to top → the bar disappears

    **TRIP-04 — Highlights bar:**
    - 4 boxes visible: date range, duration (15 giorni), price (€3.490), and status (Ultimi posti)

    **TRIP-05 — Sticky tab navigation:**
    - 4 tabs visible: Itinerario, Cosa Include, Galleria, Richiedi Preventivo
    - Clicking each tab scrolls smoothly to the correct section with correct offset
    - Active tab is styled with navy underline

    **TRIP-06 — Itinerary accordion:**
    - Day 1 ("Arrivo a Los Angeles") is open on page load, others collapsed
    - Click Day 2 → Day 1 closes, Day 2 opens
    - Day number circle has navy background with zero-padded number
    - Chevron rotates 90° on open

    **TRIP-07 — Includes/Excludes:**
    - Two-column layout on desktop: green checkmarks on left column, red X marks on right
    - Items match the trips.json arrays (7 included, 4 excluded for West America)

    **TRIP-08 — Gallery + Lightbox:**
    - Masonry/column grid shows all 4 gallery photos
    - Click any thumbnail → lightbox opens with full-screen image
    - Counter shows "1 / 4" (or correct position)
    - Left/right arrow buttons navigate; keyboard ← → also navigate
    - Esc key or clicking the dark overlay closes the lightbox
    - Swipe left/right works on mobile (if testing on mobile or DevTools)

    **TRIP-09 — Tags:**
    - Gold tag pills visible below gallery: "america", "road-trip", "aprile", "coppia", "famiglia", "avventura", "parchi-naturali"
    - Clicking a tag navigates to /viaggi with the correct filter param

    **TRIP-10 — Related trips:**
    - Section "Altri viaggi che potrebbero piacerti" is absent (no other published America trips currently) — verify the section simply does not render when there are no related trips, without a PHP error

    **FORM-01 — Quote form renders from form_config:**
    - Quote form section visible at bottom of page
    - Room type dropdown shows: Camera Doppia, Camera Singola (+€650), Camera Tripla (-€200)
    - Two add-on checkboxes: Assicurazione viaggio (€180), Upgrade Premium Economy (€450)

    **FORM-02 — Live price calculation:**
    - Initial price estimate shows €6.980 (3490 × 2 adults, Camera Doppia)
    - Change to Camera Singola → price updates to €8.280 (4140 × 2)
    - Add Assicurazione checkbox → price increases by €360 (180 × 2 people)
    - Change adult count to 3 → price updates correctly

    **FORM-03 — B2B/Agenzia toggle:**
    - Default shows "Privato" active, no agency fields
    - Click "Agenzia" → extra fields appear: Nome Agenzia, Codice IATA, Città/Provincia, Commissione

    **FORM-04 — Form submission (with no webhook configured):**
    - Fill in Nome, Cognome, Email fields
    - Click "Invia Richiesta di Preventivo"
    - Since DEFAULT_WEBHOOK_URL is empty in config.php, server returns {"success": true, "note": "no_webhook"}
    - Form is replaced with the green success message ("Richiesta inviata!")

    **FORM-05 — WhatsApp CTA:**
    - "Preferisci scrivere su WhatsApp?" link is visible below the form
    - Link opens a wa.me URL (check href contains wa.me)

    **FORM-06 — AI generator (curl test):**
    Run from project root (or any machine with PHP/curl available):
    ```
    curl -X POST http://localhost:8000/api/generate-form.php \
      -H "Content-Type: application/json" \
      -d '{"description":"Viaggio in Tailandia 10 giorni con Lorenzo da €2800, voli inclusi, hotel 4 stelle"}'
    ```
    Expected: {"success":true,"form_config":{...},"source":"default"} with price_per_person ≈ 2800
  </how-to-verify>
  <resume-signal>Type "approved" when all checks pass, or describe any issues found (e.g., "TRIP-06 accordion does not close previous day", "FORM-02 price not updating")</resume-signal>
</task>

</tasks>

<verification>
All 16 requirements manually verified by the user. Any failures are documented and addressed via gap closure.
</verification>

<success_criteria>
User types "approved". All 16 TRIP-* and FORM-* requirements confirmed working in the browser. Phase 4 is complete.
</success_criteria>

<output>
After user approval, create `.planning/phases/04-trip-detail-quote-form/04-05-SUMMARY.md`
</output>

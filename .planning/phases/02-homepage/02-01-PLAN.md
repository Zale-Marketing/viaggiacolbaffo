---
phase: 02-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/header.php
  - assets/css/style.css
autonomous: true
requirements:
  - HOME-01
  - HOME-03
  - HOME-04

must_haves:
  truths:
    - "On pages with a full-viewport hero, a visitor sees the header as semi-transparent over the dark photo — not as an opaque white bar blocking the image"
    - "When the visitor starts scrolling down a hero page, the header transitions to solid white with navy links — it becomes readable against page content"
    - "On mobile, the active trips section shows one card at ~85% width with the next card peeking at the edge, signalling that more cards can be swiped"
    - "When a visitor hovers a destination card, the photo zooms in slightly and a white border appears around the card — the interaction feels premium and responsive"
  artifacts:
    - path: "includes/header.php"
      provides: "hero_page flag support — body.has-hero class, transparent-to-solid scroll behavior"
      contains: "has-hero"
    - path: "assets/css/style.css"
      provides: "All new CSS: has-hero header state, hero section, urgency bar, trips carousel, dest cards, why-grid, testimonials, B2B banner, footer"
      contains: ".hero, .urgency-bar, .trips-carousel, .dest-card, .why-grid, .testimonial-card, .b2b-banner, .site-footer"
  key_links:
    - from: "includes/header.php"
      to: "body.has-hero"
      via: "PHP $hero_page flag conditional on <body> class"
      pattern: "has-hero"
    - from: "assets/css/style.css"
      to: "body.has-hero #site-header"
      via: "CSS transparent state + .scrolled override"
      pattern: "body\\.has-hero"
---

<objective>
Extend the shared infrastructure that all hero pages in the project will rely on. Adds the `$hero_page` PHP flag to header.php and writes all new CSS for Phase 2 sections into style.css.

Purpose: Plans 02 and 03 depend on these styles being present. The hero state machine (transparent → solid header) must work before index.php is assembled. The footer CSS must also live here so footer.php can use those classes.
Output: Modified header.php (hero flag support), extended style.css (hero, urgency bar, carousel, destination cards, why-grid, testimonials, B2B banner, footer styles).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-homepage/02-CONTEXT.md
@.planning/phases/02-homepage/02-RESEARCH.md
</context>

<interfaces>
<!-- Key existing CSS classes and variables the executor must NOT redefine -->
<!-- Extracted from assets/css/style.css -->

CSS Variables (already defined in :root):
  --primary: #000744
  --accent: #CC0031
  --gold: #CC0031       /* Note: --gold IS the red accent in this project */
  --white: #FFFFFF
  --dark: #111827
  --dark-card: #1a1f3e
  --grey: #888888
  --font-heading: 'Playfair Display', serif
  --font-body: 'Inter', sans-serif
  --radius: 12px
  --shadow: 0 4px 24px rgba(0,0,0,0.4)

Existing header CSS:
  header { background: #FFFFFF; border-bottom: 1px solid rgba(0,7,68,0.12); position: sticky; top: 0; z-index: 100; }
  header.scrolled { box-shadow: 0 2px 12px rgba(0,7,68,0.15); }
  header nav a { color: #000744; }

Existing button classes:
  .btn--gold        → navy fill (#000744), white text
  .btn--outline-white → transparent + white border, white text
  .btn-accent       → red (#CC0031) fill

Existing section/layout classes:
  .section          → padding: 5rem 0
  .section--dark    → background: var(--dark)
  .container        → max-width: 1200px, margin: 0 auto, padding: 0 1.5rem
  .section-header   → text-align: center, margin-bottom: 3rem
  .section-header__title → Playfair, red ::after underline

Existing trip classes (PERMANENT - do not modify):
  .trip-card, .trip-grid, .trip-card__image, .trip-card__overlay
  .trip-card__continent, .trip-card__status, .trip-card__content
  .trip-card__title, .trip-card__dates, .trip-card__price, .trip-card__cta
  status--confermata, status--ultimi-posti, status--sold-out, status--programmata

Existing header.php structure:
  - <body> tag currently: <body>  (no class attribute)
  - Scroll JS toggles .scrolled on #site-header (id="site-header" NOT on <header>)
    Wait: header has id="site-header" in header.php? Let's verify:
    header.php line 21: <header id="site-header">
  - Scroll JS: header.classList.toggle('scrolled', window.scrollY > 10)
    → targets var header = document.getElementById('site-header')
    → so existing CSS "header.scrolled" IS "#site-header.scrolled"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add $hero_page flag to header.php</name>
  <files>includes/header.php</files>
  <action>
    Modify the `<body>` opening tag to conditionally add `has-hero` class when `$hero_page` is set:

    Change line 19 from:
    ```html
    <body>
    ```
    To:
    ```php
    <body<?php if (!empty($hero_page)) echo ' class="has-hero"'; ?>>
    ```

    No other changes to header.php. The existing scroll JS already targets `#site-header` and toggles `.scrolled` — this is all that's needed for the CSS state machine.
  </action>
  <verify>
    <automated>php -l includes/header.php</automated>
  </verify>
  <done>header.php passes PHP lint and contains `has-hero` in the body tag conditional. Setting $hero_page = true before including header.php will produce `&lt;body class="has-hero"&gt;`.</done>
</task>

<task type="auto">
  <name>Task 2: Extend style.css with all Phase 2 section styles</name>
  <files>assets/css/style.css</files>
  <action>
    Append all new CSS to the END of assets/css/style.css. Do NOT modify any existing rules. Add a clear `/* === PHASE 2: HOMEPAGE === */` comment block. Include the following sections in order:

    **1. Has-hero header state** (transparent to solid transition):
    ```css
    /* Hero page: transparent header */
    body.has-hero #site-header {
      background: rgba(0, 0, 0, 0.3);
      border-bottom: none;
    }
    body.has-hero #site-header nav a {
      color: var(--white);
    }
    /* When scrolled — .scrolled overrides transparent state. MUST come AFTER the transparent rules */
    body.has-hero #site-header.scrolled {
      background: var(--white);
      border-bottom: 1px solid rgba(0, 7, 68, 0.12);
    }
    body.has-hero #site-header.scrolled nav a {
      color: #000744;
    }
    ```

    **2. Hero section** (full viewport, cinematic):
    - `.hero` — position: relative, height: 100vh, min-height: 600px, background-image (Unsplash mountain road URL from research), background-size: cover, background-position: center, display: flex, align-items: center, justify-content: center, text-align: center
    - `.hero__overlay` — position: absolute, inset: 0, background: linear-gradient(to bottom, rgba(0,0,0,0.55) 0%, rgba(0,0,0,0.35) 60%, rgba(0,0,0,0.6) 100%)
    - `.hero__content` — position: relative, z-index: 2, padding: 0 1.5rem, max-width: 800px
    - `.hero__tagline` — font-family: var(--font-heading), font-size: clamp(2.5rem, 6vw, 4.5rem), color: var(--white), margin: 0 0 0.5rem, text-shadow: 0 2px 12px rgba(0,0,0,0.5)
    - `.hero__subline` — font-family: var(--font-heading), font-style: italic, font-size: clamp(1.25rem, 3vw, 1.8rem), color: rgba(255,255,255,0.9), margin: 0 0 0.5rem
    - `.hero__sub` — font-size: 1rem, color: rgba(255,255,255,0.75), margin: 0 0 2rem, max-width: 500px, margin-left: auto, margin-right: auto
    - `.hero__ctas` — display: flex, gap: 1rem, justify-content: center, flex-wrap: wrap
    - DO NOT add overflow: hidden to .hero (anti-pattern — breaks sticky header in some browsers)

    **3. Urgency bar**:
    - `.urgency-bar` — background: var(--accent), color: var(--white), text-align: center, padding: 0.85rem 1.5rem, font-weight: 600, font-size: 0.95rem, display: flex, align-items: center, justify-content: center, gap: 0.75rem, flex-wrap: wrap
    - `.urgency-bar__pill` — background: rgba(255,255,255,0.2), border-radius: 20px, padding: 2px 12px, font-size: 0.85rem, font-weight: 700, letter-spacing: 0.05em, text-transform: uppercase

    **4. Trips carousel** (snap-scroll mobile, grid desktop):
    - `.trips-carousel` — display: flex, overflow-x: auto, scroll-snap-type: x mandatory, gap: 1rem, padding: 0 1.5rem 1rem, -webkit-overflow-scrolling: touch, scrollbar-width: none
    - `.trips-carousel::-webkit-scrollbar` — display: none
    - `.trips-carousel__item` — flex: 0 0 85%, scroll-snap-align: start
    - `@media (min-width: 768px)`: .trips-carousel → display: grid, grid-template-columns: repeat(2, 1fr), overflow-x: visible, padding: 0, scroll-snap-type: none; .trips-carousel__item → flex: none
    - `@media (min-width: 1024px)`: .trips-carousel → grid-template-columns: repeat(3, 1fr)

    **5. Destination cards grid**:
    - `.dest-grid` — display: grid, grid-template-columns: 1fr, gap: 1.25rem
    - `@media (min-width: 600px)`: grid-template-columns: repeat(2, 1fr)
    - `@media (min-width: 1024px)`: grid-template-columns: repeat(3, 1fr)
    - `.dest-card` — position: relative, display: block, border-radius: var(--radius), overflow: hidden, aspect-ratio: 4/3, cursor: pointer, border: 2px solid transparent, transition: border-color 0.25s ease, text-decoration: none
    - `.dest-card:hover` — border-color: var(--white)
    - `.dest-card__img` — width: 100%, height: 100%, object-fit: cover, display: block, transition: transform 0.4s ease, will-change: transform (Safari fix for overflow:hidden + border-radius + transform bug)
    - `.dest-card:hover .dest-card__img` — transform: scale(1.05)
    - `.dest-card__overlay` — position: absolute, inset: 0, background: linear-gradient(to bottom, rgba(0,0,0,0.1) 0%, rgba(0,0,0,0.65) 100%), transition: background 0.25s ease
    - `.dest-card:hover .dest-card__overlay` — background: linear-gradient(to bottom, rgba(0,0,0,0.05) 0%, rgba(0,0,0,0.5) 100%)
    - `.dest-card__content` — position: absolute, bottom: 1.25rem, left: 1.25rem, right: 1.25rem, z-index: 2
    - `.dest-card__name` — font-family: var(--font-heading), font-size: 1.4rem, color: var(--white), margin: 0 0 0.25rem, text-shadow: 0 1px 4px rgba(0,0,0,0.7)
    - `.dest-card__tagline` — font-size: 0.85rem, color: rgba(255,255,255,0.85), margin: 0, text-shadow: 0 1px 3px rgba(0,0,0,0.7)

    **6. Why-Baffo grid**:
    - `.why-grid` — display: grid, grid-template-columns: 1fr, gap: 2rem
    - `@media (min-width: 600px)`: grid-template-columns: repeat(2, 1fr)
    - `@media (min-width: 1024px)`: grid-template-columns: repeat(4, 1fr)
    - `.why-block` — text-align: center, padding: 1.5rem
    - `.why-block__icon` — color: var(--gold), margin-bottom: 1rem, font-size: 2rem
    - `.why-block__title` — font-family: var(--font-heading), font-size: 1.1rem, margin: 0 0 0.5rem
    - `.why-block__text` — color: var(--grey), font-size: 0.9rem, margin: 0, line-height: 1.6

    **7. Founder / Chi è il Baffo**:
    - `.founder-grid` — display: grid, grid-template-columns: 1fr, gap: 3rem, align-items: center
    - `@media (min-width: 768px)`: grid-template-columns: 1fr 1.5fr
    - `.founder-portrait` — width: 100%, border-radius: var(--radius), object-fit: cover, aspect-ratio: 3/4
    - `.founder-stats` — display: flex, gap: 2rem, margin: 1.5rem 0, flex-wrap: wrap
    - `.founder-stat__number` — font-family: var(--font-heading), font-size: 2.5rem, color: var(--gold), display: block, line-height: 1
    - `.founder-stat__label` — font-size: 0.8rem, color: var(--grey), text-transform: uppercase, letter-spacing: 0.05em

    **8. Testimonial cards**:
    - `.testimonials-grid` — display: grid, grid-template-columns: 1fr, gap: 1.5rem
    - `@media (min-width: 768px)`: grid-template-columns: repeat(3, 1fr)
    - `.testimonial-card` — background: var(--dark-card), border-radius: var(--radius), padding: 1.75rem, border: 1px solid rgba(255,255,255,0.06)
    - `.testimonial-card__stars` — color: var(--gold), margin-bottom: 1rem, font-size: 0.9rem
    - `.testimonial-card__text` — font-style: italic, color: rgba(255,255,255,0.85), margin: 0 0 1.25rem, line-height: 1.7
    - `.testimonial-card__author strong` — display: block, font-weight: 600
    - `.testimonial-card__author span` — font-size: 0.8rem, color: var(--grey)

    **9. B2B banner**:
    - `.b2b-banner` — margin: 0 1.5rem
    - `.b2b-banner__inner` — display: flex, align-items: center, justify-content: space-between, gap: 2rem, padding: 3rem 1.5rem, flex-wrap: wrap, background: var(--dark-card), border: 1px solid rgba(204, 0, 49, 0.4), border-radius: var(--radius)
    - `.b2b-banner__title` — font-family: var(--font-heading), font-size: clamp(1.5rem, 3vw, 2rem), margin: 0 0 0.5rem
    - `.b2b-banner__sub` — color: var(--grey), margin: 0, max-width: 500px

    **10. Production footer**:
    - `.site-footer` — background: var(--dark), color: rgba(255,255,255,0.75), padding-top: 4rem, font-size: 0.9rem
    - `.site-footer__grid` — display: grid, grid-template-columns: 1fr, gap: 2.5rem, padding-bottom: 3rem
    - `@media (min-width: 768px)`: grid-template-columns: 1.5fr 1fr 1fr
    - `.site-footer__logo` — max-height: 45px, width: auto, margin-bottom: 1rem
    - `.site-footer__tagline` — color: var(--grey), line-height: 1.6, margin: 0
    - `.site-footer__heading` — font-family: var(--font-heading), font-size: 1rem, color: var(--white), margin: 0 0 1rem, text-transform: uppercase, letter-spacing: 0.08em
    - `.site-footer__links` — list-style: none, padding: 0, margin: 0
    - `.site-footer__links li` — margin-bottom: 0.5rem
    - `.site-footer__links a, .site-footer__col a` — color: rgba(255,255,255,0.65), transition: color 0.2s ease
    - `.site-footer__links a:hover, .site-footer__col a:hover` — color: var(--white)
    - `.site-footer__col p` — margin: 0 0 0.6rem
    - `.site-footer__col i` — margin-right: 0.4rem, color: var(--gold), width: 16px, text-align: center
    - `.site-footer__social` — display: flex, gap: 1rem, margin: 1rem 0, font-size: 1.3rem
    - `.site-footer__social a` — color: rgba(255,255,255,0.65), transition: color 0.2s ease
    - `.site-footer__social a:hover` — color: var(--white)
    - `.site-footer__iata` — font-size: 0.8rem, color: var(--grey), margin: 0
    - `.site-footer__bottom` — background: var(--primary), padding: 1rem 0, text-align: center, font-size: 0.8rem, color: rgba(255,255,255,0.5)
    - `.site-footer__bottom p` — margin: 0
  </action>
  <verify>
    <automated>php -l assets/css/style.css 2>/dev/null || echo "CSS (not PHP lint applicable)"; grep -c "has-hero\|\.hero\b\|\.urgency-bar\|\.trips-carousel\|\.dest-card\|\.why-grid\|\.testimonial-card\|\.b2b-banner\|\.site-footer" assets/css/style.css</automated>
  </verify>
  <done>style.css contains all 10 new sections (has-hero, hero, urgency-bar, trips-carousel, dest-card, why-grid, founder, testimonial-card, b2b-banner, site-footer). Existing rules are untouched. `body.has-hero #site-header.scrolled` rules appear AFTER `body.has-hero #site-header` rules.</done>
</task>

</tasks>

<verification>
- `php -l includes/header.php` passes with no errors
- `grep "has-hero" includes/header.php` returns the body class conditional
- `grep -c "\.hero\b\|\.urgency-bar\|\.trips-carousel\|\.dest-card\|\.why-grid\|\.site-footer" assets/css/style.css` returns 6+ matches
- Existing `.trip-card`, `.section`, `.container`, `.btn--gold` rules are still present and unchanged
</verification>

<success_criteria>
- header.php: `<body<?php if (!empty($hero_page)) echo ' class="has-hero"'; ?>>` is present
- style.css: All 10 new CSS sections appended without modifying existing rules
- The `body.has-hero #site-header.scrolled` block appears after `body.has-hero #site-header` (specificity order)
- `will-change: transform` is present on `.dest-card__img` (Safari overflow fix)
- No `overflow: hidden` on `.hero` class
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-01-SUMMARY.md`
</output>

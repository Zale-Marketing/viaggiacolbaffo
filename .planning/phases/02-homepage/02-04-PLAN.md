---
phase: 02-homepage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [assets/css/style.css]
autonomous: true
requirements: [HOME-04]
gap_closure: true

must_haves:
  truths:
    - "Trip card minimum height on mobile is 280px — card never collapses below that"
    - "Trip card bottom content area has enough top padding to never reach the badge zone"
    - "Gradient overlay provides dark coverage from 60% downward for text readability"
  artifacts:
    - path: "assets/css/style.css"
      provides: "Fixed .trip-card, .trip-card__overlay, .trip-card__content rules"
      contains: "min-height: 280px"
  key_links:
    - from: "assets/css/style.css"
      to: ".trip-card"
      via: "min-height: 280px"
      pattern: "min-height:\\s*280px"
    - from: "assets/css/style.css"
      to: ".trip-card__content"
      via: "padding: 3.5rem 1.25rem 1.25rem"
      pattern: "padding:\\s*3\\.5rem"
    - from: "assets/css/style.css"
      to: ".trip-card__overlay"
      via: "4-stop gradient"
      pattern: "transparent 30%"
---

<objective>
Fix trip card mobile layout — three targeted CSS edits eliminate the badge/content overlap reported in UAT test 4.

Purpose: Trip card titles on mobile overlap with the top-right status pill because the card has no min-height and the content area lacks top padding to clear the badge zone.
Output: Three CSS property changes in assets/css/style.css. No HTML changes needed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-homepage/02-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply 3 CSS fixes for trip card mobile overlap</name>
  <files>assets/css/style.css</files>
  <action>
Make exactly three Edit operations on assets/css/style.css:

**Edit 1 — .trip-card (line ~149): add min-height**

Current block (lines 149-156):
```css
.trip-card {
  position: relative;
  border-radius: var(--radius);
  overflow: hidden;
  background: var(--dark-card);
  box-shadow: var(--shadow);
  transition: var(--transition);
}
```

Replace with:
```css
.trip-card {
  position: relative;
  border-radius: var(--radius);
  overflow: hidden;
  background: var(--dark-card);
  box-shadow: var(--shadow);
  transition: var(--transition);
  min-height: 280px;
}
```

**Edit 2 — .trip-card__overlay (line ~173): replace 2-stop gradient with 4-stop**

Current line:
```css
  background: linear-gradient(to bottom, transparent 0%, rgba(0, 0, 0, 0.85) 100%);
```

Replace with:
```css
  background: linear-gradient(to bottom, transparent 0%, transparent 30%, rgba(0, 0, 0, 0.75) 60%, rgba(0, 0, 0, 0.92) 100%);
```

**Edit 3 — .trip-card__content (line ~229): add top padding guard**

Current line:
```css
  padding: 1.25rem;
```

Replace with:
```css
  padding: 3.5rem 1.25rem 1.25rem;
```

Do NOT change any other CSS. Do NOT reformat surrounding rules.
  </action>
  <verify>
    <automated>
grep -n "min-height: 280px" /c/Users/Zanni/viaggiacolbaffo/assets/css/style.css && grep -n "transparent 30%" /c/Users/Zanni/viaggiacolbaffo/assets/css/style.css && grep -n "3\.5rem 1\.25rem 1\.25rem" /c/Users/Zanni/viaggiacolbaffo/assets/css/style.css
    </automated>
  </verify>
  <done>
All three grep commands return matches:
- min-height: 280px found in .trip-card block
- transparent 30% found in .trip-card__overlay gradient
- 3.5rem 1.25rem 1.25rem found in .trip-card__content padding
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit the fix</name>
  <files></files>
  <action>
Stage and commit the single changed file:

```bash
git add assets/css/style.css
git commit -m "fix(02): trip card mobile layout — min-height, content padding-top, gradient"
```
  </action>
  <verify>
    <automated>git log --oneline -1</automated>
  </verify>
  <done>Most recent commit message is "fix(02): trip card mobile layout — min-height, content padding-top, gradient"</done>
</task>

</tasks>

<verification>
grep -n "min-height: 280px" assets/css/style.css
grep -n "transparent 30%" assets/css/style.css
grep -n "3\.5rem 1\.25rem 1\.25rem" assets/css/style.css

All three must return exactly one match each. If any grep returns nothing, the corresponding edit was not applied correctly.
</verification>

<success_criteria>
- .trip-card has min-height: 280px — card never collapses on mobile
- .trip-card__overlay gradient: transparent 0%, transparent 30%, rgba(0,0,0,0.75) 60%, rgba(0,0,0,0.92) 100%
- .trip-card__content padding: 3.5rem 1.25rem 1.25rem — 3.5rem top guard clears the ~44px badge zone
- Commit pushed with message: fix(02): trip card mobile layout — min-height, content padding-top, gradient
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-04-SUMMARY.md` with what was changed, which lines, and the UAT gap this closes (test 4: trip card mobile overlap).
</output>

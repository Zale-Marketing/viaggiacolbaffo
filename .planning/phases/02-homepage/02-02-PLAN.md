---
phase: 02-homepage
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified: []
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04

must_haves:
  truths:
    - "The hero section fills 100vh with a cinematic dark photo, the tagline 'Viaggia col Baffo', subline 'E non cambi mai più', and both CTAs (gold 'Scopri i viaggi' + outline-white 'Sei un'agenzia?') are visible without scrolling"
    - "The urgency bar appears immediately below the hero with red background and text 'West America Aprile 2026 — Ultimi 5 posti disponibili'"
    - "The active trips section reads published=true trips from trips.json and renders them using .trips-carousel (snap-scroll mobile, grid desktop)"
    - "The destinations section renders 6 destination cards in a 3x2 desktop / 2x3 tablet / 1-col mobile grid, each linking to destinazione.php?slug="
  artifacts:
    - path: "assets/css/style.css"
      provides: "All Phase 2 CSS sections verified present before index.php is written"
      contains: "hero__tagline, urgency-bar, trips-carousel, dest-grid"
    - path: "includes/functions.php"
      provides: "load_trips() available for use in index.php (Plan 03)"
      contains: "load_trips"
  key_links:
    - from: "index.php (written in Plan 03)"
      to: "includes/config.php + includes/functions.php"
      via: "require_once at top of file"
      pattern: "require_once.*config\\.php"
    - from: "index.php (written in Plan 03)"
      to: "trips.json via load_trips()"
      via: "array_filter($all_trips, fn($t) => $t['published'] === true)"
      pattern: "published.*true"
    - from: "index.php (written in Plan 03)"
      to: "destinazione.php"
      via: "href in each dest-card anchor"
      pattern: "destinazione\\.php\\?slug="
---

<objective>
Verify the CSS infrastructure from Plan 01 is complete and the PHP data functions are operational before Plan 03 writes the complete index.php in a single pass.

Purpose: Plan 03 Task 1 will write all of index.php as one complete file write. This verification plan confirms that all CSS classes and PHP functions it depends on actually exist — catching any gap in Plan 01 output before the file is written, not after.
Output: No new files. Verification signals that plan 03 can proceed safely.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-homepage/02-CONTEXT.md
@.planning/phases/02-homepage/02-RESEARCH.md
@.planning/phases/02-homepage/02-01-SUMMARY.md
</context>

<interfaces>
<!-- PHP functions available from includes/functions.php -->
load_trips()           → returns array of all trips from data/trips.json
get_trip_by_slug($slug) → returns single trip or false
load_tags()            → returns all tags from data/tags.json

<!-- PHP constants from includes/config.php -->
ROOT                   → absolute path to project root (__DIR__ . '/..')
WHATSAPP_NUMBER        → '+39 XXX XXXXXXX' (placeholder)

<!-- Existing CSS classes to use (from .trip-card permanent set) -->
.trip-card             → card wrapper (position: relative, overflow: hidden, border-radius, min-height: 340px)
.trip-card__image      → full-bleed img (position: absolute, inset: 0, object-fit: cover)
.trip-card__overlay    → dark gradient overlay (position: absolute, inset: 0)
.trip-card__continent  → gold pill badge top-left
.trip-card__status     → pill top-right; pair with status--{confermata|ultimi-posti|sold-out|programmata}
.trip-card__content    → bottom content area
.trip-card__title      → Playfair trip name
.trip-card__dates      → dates line
.trip-card__price      → price line
.trip-card__cta        → CTA link (use .btn.btn--gold)

<!-- New CSS from Plan 01 — must all be present before Plan 03 proceeds -->
.hero, .hero__overlay, .hero__content, .hero__tagline, .hero__subline, .hero__sub, .hero__ctas
.urgency-bar, .urgency-bar__pill
.trips-carousel, .trips-carousel__item
.dest-grid, .dest-card, .dest-card__img, .dest-card__overlay, .dest-card__content, .dest-card__name, .dest-card__tagline

<!-- Existing utility classes -->
.btn.btn--gold, .btn.btn--outline-white
.section, .section--dark, .container, .section-header, .section-header__title, .section-header__subtitle
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify CSS infrastructure is complete</name>
  <files></files>
  <action>
    Run verification commands to confirm Plan 01 output is complete and all CSS classes that index.php will depend on are present in assets/css/style.css.

    Check that these class selectors all appear in style.css:
    - `.hero` (hero section)
    - `.urgency-bar` (urgency bar)
    - `.trips-carousel` (carousel wrapper)
    - `.dest-card` (destination cards)
    - `.why-grid` (why-Baffo section)
    - `.testimonial-card` (testimonials)
    - `.b2b-banner` (B2B banner)
    - `.site-footer` (footer)
    - `body.has-hero` (header transparency rule)

    Also verify header.php has the `$hero_page` flag:
    - `grep "has-hero" includes/header.php` must return a match

    If any check fails, do NOT proceed to Plan 03 — fix the missing CSS in assets/css/style.css first (the action belongs in Plan 01's Task 2, so re-run that task or append the missing rules).

    No files are written in this task. This is a read-only verification step.
  </action>
  <verify>
    <automated>grep -c "\.hero\b\|\.urgency-bar\|\.trips-carousel\|\.dest-card\|\.why-grid\|\.testimonial-card\|\.b2b-banner\|\.site-footer\|body\.has-hero" assets/css/style.css && grep -c "has-hero" includes/header.php</automated>
  </verify>
  <done>grep returns 9+ matches in style.css confirming all Phase 2 CSS sections are present. grep returns 1 match in header.php confirming the $hero_page flag is wired. Plan 03 is safe to proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify PHP data layer is operational</name>
  <files></files>
  <action>
    Confirm that load_trips() returns data correctly and that published=true filtering works — this is the exact pattern that Plan 03's index.php will use.

    Run the PHP one-liner that filters active trips. This validates:
    1. includes/config.php loads without errors
    2. includes/functions.php loads without errors
    3. load_trips() returns the full trips array from data/trips.json
    4. array_filter with the published===true check works and returns at least 1 trip

    If the count is 0, check data/trips.json has at least one trip with `"published": true`. The sample "West America Aprile 2026" trip (DATA-04) should be present.

    No files are written in this task. This is a read-only verification step.
  </action>
  <verify>
    <automated>php -r "require 'includes/config.php'; require ROOT . '/includes/functions.php'; \$t=array_values(array_filter(load_trips(),fn(\$x)=>\$x['published']===true)); echo count(\$t).' active trips loaded\n'; if(count(\$t)===0){exit(1);}"</automated>
  </verify>
  <done>PHP one-liner prints "1 active trips loaded" (or more) and exits 0. config.php, functions.php, and trips.json all load cleanly. Plan 03 can proceed to write index.php knowing the data layer works.</done>
</task>

</tasks>

<verification>
- `grep -c "\.hero\b\|\.urgency-bar\|\.trips-carousel\|\.dest-card\|body\.has-hero" assets/css/style.css` returns 5+
- `grep "has-hero" includes/header.php` returns 1 match
- PHP active trips count is >= 1
- No new files were written by this plan (files_modified is empty)
</verification>

<success_criteria>
- All Phase 2 CSS sections confirmed present in style.css (9 grep matches minimum)
- header.php hero flag confirmed present
- load_trips() returns at least 1 published trip
- Plan 03 can now write the complete index.php as a single atomic file creation
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-02-SUMMARY.md`
</output>

---
phase: 06-admin-panel
plan: 07
type: execute
wave: 4
depends_on:
  - "06-02"
  - "06-03"
  - "06-04"
  - "06-05"
  - "06-06"
files_modified: []
autonomous: false
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05
  - ADMIN-06
  - ADMIN-07
  - ADMIN-08
  - ADMIN-09
  - ADMIN-10
  - ADMIN-11

must_haves:
  truths:
    - "Lorenzo can log in, create a trip, fill all fields, publish it, and see it live on the homepage without touching any files"
    - "Deleting a trip moves it to trash; trash can be restored or permanently cleared"
    - "Settings changes (urgency bar text, webhook URLs) are reflected on the public site immediately after saving"
    - "The AI form generator returns a usable form_config (or sensible default if no API key)"
    - "Trip preview works for unpublished drafts; invalid slugs still return 404"
  artifacts:
    - path: "admin/login.php"
    - path: "admin/index.php"
    - path: "admin/edit-trip.php"
    - path: "admin/settings.php"
    - path: "admin/tags.php"
    - path: "admin/destinations.php"
  key_links:
    - from: "admin/index.php publish toggle"
      to: "homepage trip grid"
      via: "save_trips() → trips.json → index.php load_trips() on next page load"
      pattern: "published"
---

<objective>
Human verification checkpoint: Lorenzo (or the developer acting as Lorenzo) walks through the complete admin panel using the browser checklist below. All 11 ADMIN-* requirements must pass before phase 6 is marked complete.

Purpose: The admin panel is the last phase of v1. This checkpoint confirms the site is fully operational and ready for Lorenzo to use in production.
Output: Human approval — "approved" or list of issues.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-panel/06-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete admin panel with:
- Login page with session auth (admin/login.php) — ADMIN-01
- Trip dashboard with stats, DnD table, publish toggle, soft-delete, trash (admin/index.php) — ADMIN-02
- Full trip edit form with 5 tabs: Info Base, Media, Contenuto, Itinerario, Form Config (admin/edit-trip.php) — ADMIN-03 through ADMIN-10
- Site settings page writing to admin-config.json (admin/settings.php) — ADMIN-11
- Tag management with category grouping and cascade delete (admin/tags.php)
- Destination content editor backed by destinations.json (admin/destinations.php)
- Public site updated: viaggio.php published/deleted gates + preview token, urgency bar from settings, footer company data from settings, Anthropic AI in generate-form.php, destination pages using live JSON
  </what-built>
  <how-to-verify>
Work through this checklist in the browser. The site must be deployed to SiteGround first (push to main triggers the GitHub Actions FTP deploy).

**Auth (ADMIN-01)**
[ ] Visit /admin — redirects to /admin/login.php without a session
[ ] Enter wrong password → "Password errata." error shown, stay on login
[ ] Enter correct password (Admin2025! unless changed in settings) → redirected to /admin/

**Dashboard (ADMIN-02)**
[ ] Stats bar shows correct total/published/draft counts
[ ] All trips listed with Title, Continent, Tags (chips), Status pill, Actions
[ ] Drag a trip row to a new position → row moves, toast "Salvato" appears
[ ] Reload page → new order persists (verify trips.json position field updated)
[ ] Toggle a trip's published status → pill updates instantly
[ ] "Crea Nuovo Viaggio" button → navigates to edit-trip.php?new=1

**Edit Form — Info Base tab (ADMIN-03)**
[ ] Title auto-generates slug on blur (for new trips)
[ ] Slug field is read-only for published trips (confirm with "West America" trip)
[ ] All selects populate correctly: Continent, Status
[ ] Dates, duration auto-calculation, price, commission rate fields visible
[ ] Save "Salva Bozza" → trip saved as draft; reload confirms published: false

**Edit Form — Media tab (ADMIN-04)**
[ ] Paste a valid Unsplash URL in hero image field → thumbnail appears immediately below
[ ] Add 3 gallery URLs (one per line) → 3 thumbnails appear in preview grid

**Edit Form — Contenuto tab (ADMIN-05)**
[ ] Type in short description field → char counter updates (e.g. "45 / 160")
[ ] Type past 140 chars → counter turns red
[ ] Includes/excludes textareas accept multi-line input

**Edit Form — Itinerario tab (ADMIN-06)**
[ ] "Aggiungi Giorno" adds a new row with correct auto day number
[ ] Drag a day row → day numbers renumber
[ ] Up/down arrows also reorder rows
[ ] Remove button deletes a row and renumbers remaining

**Edit Form — Tags (ADMIN-08)**
[ ] Predefined tags shown as clickable pills; clicking toggles gold fill
[ ] Type a custom tag name + press Add → new pill appears
[ ] Save trip → verify tags array in trips.json (via browser or admin display)

**Edit Form — Form Config tab (ADMIN-09)**
[ ] Form Config tab shows AI description textarea and "Genera Form con AI" button
[ ] Enter a short description → click Generate → loading spinner appears → JSON populates textarea
[ ] "Salva Form Config" saves form_config to trip (check trips.json or re-open edit page)
[ ] Webhook URL field saves correctly inside form_config.webhook_url

**Save actions (ADMIN-10)**
[ ] "Salva Bozza" → published: false, trip not shown on homepage
[ ] "Pubblica" → published: true, trip appears on homepage trip grid immediately
[ ] "Anteprima" button → opens /viaggio/{slug}?preview={token} in new tab even for drafts
[ ] Visit /viaggio/{slug} (no preview token) for a draft → 404
[ ] "Rigenera token" → new token generated, old preview URL no longer works

**Soft-delete + Trash**
[ ] Click "Elimina" on a published trip → modal asks to unpublish first
[ ] Unpublish, then Elimina → modal confirms "Cestino" → trip disappears from active list
[ ] Cestino section appears with the deleted trip
[ ] "Ripristina" → trip returns to active list
[ ] Create a draft, delete it, then "Svuota Cestino" → all trashed trips permanently removed

**Settings (ADMIN-11)**
[ ] /admin/settings.php loads with all current config values pre-populated
[ ] Change "Testo barra urgenza" to "TEST — nuova urgenza" → Save → visit homepage → urgency bar shows new text
[ ] Change company name → Save → visit homepage footer → new company name shown
[ ] Enter a new admin password → Save → Logout → log back in with new password (test!)
[ ] Reset urgency bar and company name back to originals after testing

**Tag management**
[ ] /admin/tags.php: tags grouped by category (Continente, Tipo Viaggio, Per Chi, Mese)
[ ] Add a new test tag "Test Tag" in category "altro" → appears in list
[ ] Delete the test tag → disappears from list; verify it's removed from any trip's tags array

**Destination editing**
[ ] /admin/destinations.php: shows 6 destination tiles
[ ] Click "Modifica" on America → edit form loads with current data
[ ] Change first intro paragraph text → Save → visit /destinazione?slug=america → new text appears

**No regressions on public site**
[ ] /viaggi (catalog) — loads, filtering works
[ ] /viaggio/west-america-aprile-2026 — loads (published trip)
[ ] /destinazione?slug=america — loads with editorial content
[ ] /agenzie — loads
  </how-to-verify>
  <resume-signal>Type "approved" when all checklist items pass, or describe any issues found so they can be fixed.</resume-signal>
</task>

</tasks>

<verification>
Human browser verification using the checklist above.
</verification>

<success_criteria>
All 35+ checklist items pass in the browser. Lorenzo can perform the full workflow: login → create trip → fill all fields → publish → see live on site → manage settings → without touching any code or server files.
</success_criteria>

<output>
After approval, create `.planning/phases/06-admin-panel/06-07-SUMMARY.md` and update ROADMAP.md Phase 6 status to "Complete".
</output>

---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - assets/css/style.css
  - assets/js/main.js
  - includes/header.php
  - includes/footer.php
  - design-preview.php
autonomous: false
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
  - DESIGN-05

must_haves:
  truths:
    - "All CSS design tokens are defined as custom properties on :root: --black, --gold, --gold-light, --white, --dark, --dark-card, --grey, status colors, --font-heading, --font-body, --radius, --shadow, --transition"
    - "Google Fonts (Playfair Display + Inter) and Font Awesome 6 load from CDN"
    - "The trip card component renders full-bleed image with gradient overlay, continent badge (top-left, gold pill), status pill (top-right), Playfair title, dates, price, and CTA link"
    - "Section headers use Playfair Display with a gold ::after accent underline"
    - "The CSS grid renders 1-column on mobile (< 768px), 2-column on tablet (768px-1023px), 3-column on desktop (>= 1024px)"
    - "design-preview.php renders all design tokens visually so Lorenzo can sign off before Phase 2 begins"
  artifacts:
    - path: "assets/css/style.css"
      provides: "Complete design system — variables, base, typography, components, layout"
      contains: "--gold: #C9A84C"
    - path: "assets/js/main.js"
      provides: "Minimal JS stub — shared behaviors added in later phases"
    - path: "includes/header.php"
      provides: "Shared HTML head with CDN links, nav stub — included by all pages"
      contains: "fonts.googleapis.com"
    - path: "includes/footer.php"
      provides: "Shared footer HTML + closing body/html tags"
    - path: "design-preview.php"
      provides: "Visual validation page showing all design tokens, typography, cards, grid"
  key_links:
    - from: "includes/header.php"
      to: "assets/css/style.css"
      via: "<link rel='stylesheet'> tag"
      pattern: "style\\.css"
    - from: "includes/header.php"
      to: "Google Fonts CDN"
      via: "preconnect + stylesheet link"
      pattern: "fonts\\.googleapis\\.com"
    - from: "design-preview.php"
      to: "includes/header.php"
      via: "require_once ROOT . '/includes/header.php'"
      pattern: "require.*header\\.php"
    - from: "assets/css/style.css"
      to: ":root CSS variables"
      via: "All component styles reference variables, never raw hex values"
      pattern: "var\\(--"
---

<objective>
Build the complete CSS design system and the shared PHP page scaffolding (header.php, footer.php) that all future pages include. Produce design-preview.php as the visual validation artifact for Phase 1 sign-off.

Purpose: Every page in phases 2-5 includes header.php and footer.php and uses classes defined here. The design system must be complete and correct before any page is built on top of it.

Output: style.css with full token set and all component classes, header.php and footer.php stubs, main.js stub, design-preview.php demonstrating the full design system.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — header.php and design-preview.php depend on these -->
```php
// config.php constants (always require_once config.php before header.php)
define('ROOT', __DIR__ . '/..');  // Absolute project root

// Correct include pattern for ALL pages (subdirectory-safe):
require_once ROOT . '/includes/config.php';
require_once ROOT . '/includes/functions.php';
require_once ROOT . '/includes/header.php';
// ... page content ...
require_once ROOT . '/includes/footer.php';
```

<!-- CSS class names defined here are consumed by ALL future phases — do not rename after Phase 1 -->
Trip card HTML structure (Phases 2, 3, 4 will reuse this):
```html
<div class="trip-card">
  <img class="trip-card__image" src="..." alt="...">
  <div class="trip-card__overlay"></div>
  <span class="trip-card__continent">America</span>
  <span class="trip-card__status status--ultimi-posti">Ultimi Posti</span>
  <div class="trip-card__content">
    <h3 class="trip-card__title">West America Aprile 2026</h3>
    <p class="trip-card__dates">5 Apr - 19 Apr 2026</p>
    <p class="trip-card__price">Da 3.490</p>
    <a class="trip-card__cta btn btn--gold" href="/viaggio/west-america-aprile-2026">Scopri il viaggio</a>
  </div>
</div>
```

Section header HTML structure:
```html
<div class="section-header">
  <h2 class="section-header__title">I Nostri Viaggi</h2>
  <p class="section-header__subtitle">Subtitle text here</p>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: style.css, main.js, header.php, footer.php</name>
  <files>assets/css/style.css, assets/js/main.js, includes/header.php, includes/footer.php</files>
  <action>
Create `assets/css/style.css` with the following organized sections (use /* === SECTION === */ comment headers):

**Section 1: CSS Variables**
Define ALL tokens on :root exactly as specified in DESIGN-01:
```css
:root {
  --black: #0D0D0D;
  --gold: #C9A84C;
  --gold-light: #e8c76a;
  --white: #FFFFFF;
  --dark: #1a1a1a;
  --dark-card: #222222;
  --grey: #888888;
  --status-green: #2ecc71;
  --status-orange: #e67e22;
  --status-red: #e74c3c;
  --font-heading: 'Playfair Display', serif;
  --font-body: 'Inter', sans-serif;
  --radius: 12px;
  --shadow: 0 4px 24px rgba(0,0,0,0.4);
  --transition: all 0.3s ease;
}
```

**Section 2: Base / Reset**
- box-sizing: border-box on *, margin/padding 0 on body
- body: background var(--black), color var(--white), font-family var(--font-body), line-height 1.6
- a: color inherit, text-decoration none
- img: max-width 100%, height auto, display block

**Section 3: Typography**
- h1-h4: font-family var(--font-heading), line-height 1.2
- .section-header: text-align center, margin-bottom 3rem, padding 0 1rem
- .section-header__title: font-size clamp(1.75rem, 4vw, 2.5rem), position relative, display inline-block, padding-bottom 0.75rem, margin-bottom 0.5rem
- .section-header__title::after: content '', position absolute, bottom 0, left 50%, transform translateX(-50%), width 3rem, height 3px, background var(--gold) (gold accent underline per DESIGN-05)
- .section-header__subtitle: color var(--grey), font-size 1.1rem

**Section 4: Buttons**
- .btn: display inline-block, padding 0.75rem 1.75rem, border-radius var(--radius), font-weight 600, transition var(--transition), cursor pointer, border none
- .btn--gold: background var(--gold), color var(--black), hover: background var(--gold-light)
- .btn--outline-white: border 2px solid var(--white), color var(--white), background transparent, hover: background rgba(255,255,255,0.1)

**Section 5: Trip Cards** (these class names are PERMANENT — phases 2-4 reuse them)
Use the CSS from RESEARCH.md Pattern 7 as the base. Add:
- .trip-card__content: position absolute, bottom 0, padding 1.25rem, width 100%
- .trip-card__title: font-family var(--font-heading), font-size 1.15rem, margin-bottom 0.25rem, color var(--white)
- .trip-card__dates: font-size 0.8rem, color rgba(255,255,255,0.8), margin-bottom 0.25rem
- .trip-card__price: font-size 1rem, font-weight 700, color var(--gold), margin-bottom 0.75rem
- Status modifier classes: .status--confermata (green bg), .status--ultimi-posti (orange bg), .status--sold-out (red bg), .status--programmata (grey bg)

**Section 6: Grid Layout**
Mobile-first per RESEARCH.md Pattern 6:
- .trip-grid: display grid, grid-template-columns 1fr, gap 1.5rem
- @media (min-width: 768px): grid-template-columns repeat(2, 1fr)
- @media (min-width: 1024px): grid-template-columns repeat(3, 1fr)

**Section 7: Container / Layout utilities**
- .container: max-width 1200px, margin 0 auto, padding 0 1.5rem
- .section: padding 5rem 0
- .section--dark: background var(--dark)

Create `assets/js/main.js` as a minimal stub:
```js
// main.js — Shared JavaScript behaviors
// Phase-specific interactions are added as inline scripts in later phases
document.addEventListener('DOMContentLoaded', function() {
  // Shared behaviors initialized here as phases add them
});
```

Create `includes/header.php`:
- DOCTYPE html, lang="it", charset="UTF-8"
- viewport meta tag
- Page title: use `$page_title ?? 'Viaggia col Baffo'` (pages set $page_title before including header)
- Google Fonts preconnect + stylesheet link (Playfair Display 400,700,italic 400; Inter 400,500,600) with `?display=swap` parameter. Use preconnect to fonts.googleapis.com AND to fonts.gstatic.com with `crossorigin` attribute (the crossorigin is required on gstatic or the preconnect is ignored for CORS font requests)
- Font Awesome 6.5.0 CDN from cdnjs.cloudflare.com
- Stylesheet link using absolute path: `<link rel="stylesheet" href="/assets/css/style.css">`
- Opening `<body>`, minimal `<header>` nav: logo text "Viaggia col Baffo" linking to /, nav links Viaggi (/viaggi), Agenzie (/agenzie). Keep nav minimal — full nav is Phase 2.
- `<main>` opening tag

Create `includes/footer.php`:
- Close `</main>`
- Footer element: copyright "2025 Viaggia col Baffo - Y86 Travel. P.IVA placeholder."
- Script tag: `<script src="/assets/js/main.js"></script>`
- `</body></html>`
  </action>
  <verify>
    <automated>php -l "C:/Users/Zanni/viaggiacolbaffo/includes/header.php" && php -l "C:/Users/Zanni/viaggiacolbaffo/includes/footer.php" && echo "header/footer syntax OK"</automated>
  </verify>
  <done>style.css contains all :root variables from DESIGN-01 with exact hex values. All 7 CSS sections exist with comment headers. header.php and footer.php pass php -l. Google Fonts CDN link uses preconnect pattern with crossorigin on fonts.gstatic.com. Font Awesome 6.5.0 CDN link present.</done>
</task>

<task type="auto">
  <name>Task 2: design-preview.php visual validation artifact</name>
  <files>design-preview.php</files>
  <action>
Create `design-preview.php` — a standalone page that demonstrates every design token so Lorenzo can sign off visually before Phase 2.

This page includes header.php and footer.php and contains these 7 sections in order:

1. **Warning banner** — amber/yellow background bar at top of page content: "DESIGN PREVIEW — Phase 1 validation page. Delete or protect after sign-off."

2. **Color Swatches** — one colored square (80x80px inline style) per token, labeled with variable name and hex value. Include: --black (#0D0D0D), --gold (#C9A84C), --gold-light (#e8c76a), --white (#FFFFFF, add 1px grey border), --dark (#1a1a1a), --dark-card (#222222), --grey (#888888), --status-green (#2ecc71), --status-orange (#e67e22), --status-red (#e74c3c).

3. **Typography Scale** — h1 through h4 using var(--font-heading) applied inline, then a paragraph of body text in var(--font-body). Label each.

4. **Section Header Component** — real .section-header div: title "I Nostri Viaggi", subtitle "Ogni viaggio e' un'esperienza unica con Lorenzo". The gold ::after underline should be visible.

5. **Buttons** — .btn.btn--gold and .btn.btn--outline-white side by side with flex gap.

6. **Trip Card — single card** — hardcoded .trip-card using West America data. Image: "https://images.unsplash.com/photo-1549880338-65ddcdfd017b?w=800". Show continent badge "America" (gold pill, top-left), status pill "Ultimi Posti" (orange, top-right, class status--ultimi-posti), title in Playfair, dates "5 Apr - 19 Apr 2026", price "Da 3.490", CTA button. Wrap in a div with max-width: 380px to prevent it from stretching full width.

7. **Trip Grid — 3 cards** — .trip-grid container with 3 hardcoded .trip-card instances showing all 3 status states: one confermata (green "Confermata"), one ultimi-posti (orange "Ultimi Posti"), one sold-out (red "Sold Out"). Use different Unsplash images for visual variety. This is the primary check for DESIGN-03 and DESIGN-04.

8. **Font Awesome Icons** — flex row showing icons with labels: fa-check, fa-xmark, fa-brands fa-whatsapp, fa-location-dot, fa-calendar, fa-user, fa-star, fa-chevron-down.

Page structure:
```php
<?php
require_once __DIR__ . '/includes/config.php';
$page_title = 'Design Preview - Phase 1';
require_once ROOT . '/includes/header.php';
?>
<!-- 7 sections here -->
<?php require_once ROOT . '/includes/footer.php'; ?>
```
  </action>
  <verify>
    <automated>php -l "C:/Users/Zanni/viaggiacolbaffo/design-preview.php" && echo "design-preview.php syntax OK"</automated>
  </verify>
  <done>design-preview.php passes php -l. File contains all 7 sections. All three status modifier classes (status--confermata, status--ultimi-posti, status--sold-out) are present. Page uses require_once with ROOT constant pattern.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual sign-off on design system</name>
  <action>Human visual verification of design-preview.php. No code changes in this step — run the PHP dev server and visually inspect all design sections.</action>
  <what-built>
Complete CSS design system (style.css with all tokens, card component, grid, typography), shared page scaffold (header.php with CDN links, footer.php), JS stub, and design-preview.php visual validation page.
  </what-built>
  <how-to-verify>
1. Run `php -S localhost:8000` from the project root and visit http://localhost:8000/design-preview.php
2. Check colors: all 10 color swatches visible; gold swatch is a warm gold (#C9A84C), not bright yellow
3. Check typography: h1 heading renders in Playfair Display (elegant serif), body text in Inter (clean sans-serif)
4. Check section header: "I Nostri Viaggi" has a short gold underline centered below it
5. Check single trip card: full-bleed landscape photo, dark gradient overlay at bottom, "America" gold badge top-left, orange "Ultimi Posti" badge top-right, title in Playfair, price in gold, CTA button
6. Check grid: at > 1024px width you see 3 columns; at 768px-1024px you see 2 columns; at < 768px you see 1 column
7. Check icons: Font Awesome icons render as proper glyphs (not squares)
8. Check fonts in DevTools Network tab: requests to fonts.googleapis.com complete with status 200
  </how-to-verify>
  <verify>
    <automated>echo "Human checkpoint - no automated verification"</automated>
  </verify>
  <done>Lorenzo has approved the design-preview.php rendering by typing 'approved'</done>
  <resume-signal>Type "approved" if the design looks correct, or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
Local syntax checks:
- `php -l includes/header.php` passes
- `php -l includes/footer.php` passes
- `php -l design-preview.php` passes

Content checks:
- style.css contains `--gold: #C9A84C` (exact value)
- style.css contains all 3 grid breakpoint rules (base 1fr, 768px 2-col, 1024px 3-col)
- style.css contains .trip-card__status and all 4 status modifier classes
- header.php contains `crossorigin` on the fonts.gstatic.com preconnect link
- design-preview.php sets $page_title before requiring header.php
</verification>

<success_criteria>
- style.css defines all DESIGN-01 CSS custom properties with exact values
- Google Fonts (Playfair Display + Inter) and Font Awesome 6 are linked via CDN in header.php
- .trip-grid CSS renders 1/2/3 columns at correct breakpoints (DESIGN-03)
- .trip-card component has all required visual elements: image, overlay, badges, content (DESIGN-04)
- .section-header__title::after produces gold underline (DESIGN-05)
- design-preview.php renders correctly in browser
- Lorenzo gives approval via checkpoint
- All PHP files pass php -l
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
